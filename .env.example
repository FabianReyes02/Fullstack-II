# Copy to .env.local and set your secret key for frontend encryption
REACT_APP_SECRET_KEY=tu_secret_compartida_aqui

# API base URL used by the frontend in production (no trailing slash)
REACT_APP_API_URL=http://localhost:5000
